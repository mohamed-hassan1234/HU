NODE_ENV=development
PORT=5020
MONGO_URI=mongodb://127.0.0.1:27017/hucems_db
JWT_SECRET=change_this_hucems_secret
JWT_EXPIRES_IN=1h
CLIENT_URL=https://www.ctes.hu.edu.so,https://ctes.hu.edu.so
# Development only. Set temporarily to true only when intentionally running npm run seed.
ALLOW_DATABASE_RESET=false
